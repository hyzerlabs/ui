node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright
test-results
playwright-report
/e2e

# Changesets (keep config; ignore generated files in CI)
# .changeset/ is tracked (config + pending changesets)

# Vitest browser cache
.vitest-cache

# Generated icons (specs/36 R1) — regenerated by `pnpm gen:icons`
# (wired into `prepare` and as a pre-step of `package`); never committed.
/src/lib/icons/generated

# Agent worktrees and local session state — each worktree carries its own
# node_modules, so this must never be committed.
.claude/

# Scratch dir for scripts/gen-sizes.ts (bundle measurement)
.size-tmp/
