/**
 * Design tokens — placeholder for Sprint 1.
 * CSS custom properties are the primary token format; this JS module
 * exposes token names and metadata for tooling.
 */
export const tokens = {
	/** Token namespace prefix */
	prefix: '--hz'
} as const;
